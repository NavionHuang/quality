1、装外箱，装卡板（只通过数据方式查询，需要进行工作：新建数据源，查询数据，解析到前端展示），报价3000元，天数3天
2、保修卡、彩盒等录入地方，通过导入工单即可（已有工单不会导入，不存在会新增工单，bom数据存在不导入，不存在新增，注pom是已产品类型定义的，工单也是已某个产品类型）---已有功能
3、作业人工作台增加统计扫描的Ok数量NG数量，如统计某个作业员本次登陆开始的作业数量，可以增加（1天），如果是统计整个工单的化会每次查询统计内容，会影响作业效率（同时几十人操作不断的统计，系统压力是否会影响作业的效率，影响多少不可估量），建议不统计全局
4、工作日志可以最新的在最上面
5、增加拉线绑定到拉长角色，（系统只有管理员，维修员，作业员）建议使用维修人员角色（原因，人员是不绑定到产线上的）如果修改产线绑定到人员上，首先需要修改用户功能，第二修改作业功能，第三修改查询统计功能
6、绑定维修人员到产线上，维修人员只能维修此产线上的数据。（于拉长类似修改）
（5，6功能，可以修改需求，但是此需求涉及内容较多，需要一个星期左右完成和检查影响其他功能模块）
7、新增维修作业界面，维修功能已满足当前维修功能，如增加此界面需要重新开发此页面（维修人员登陆后到维修界面的话，怎么调整查询作业等功能），
如必须变更此功能，需要3-4天开发时间，请斟酌是否变更
8、维修工站是否必须返回指定的开始工站，还是从哪里异常就返回哪里？是否需要限定，第二个当时需求维修人员为了不影响作业有可能不把产品放回流程，
自己录入数据（当前的维修功能），现在变更必须返回流程里录入数据（当前维修站不下一步录入工站数据）
